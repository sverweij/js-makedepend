var assert = require("assert");
var core = require("../src/makedepend.js");
var fs = require("fs");

describe('#makedepend', function() {
    it("dummy to make coverage report more realistic", function() {
        var lActual = true;
        var lExpected = true;
        assert.equal(lActual, lExpected);
    });
});
