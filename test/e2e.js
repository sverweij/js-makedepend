var assert = require("assert");
var core = require("../src/core.js");
var fs = require("fs");

    describe('#core - main', function() {
        it('should do stuff', function() {
            assert.equal("same"/* actual */, "same" /* expected */);
        });
    });
